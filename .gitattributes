# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must stay LF (they run on the Linux server).
*.sh text eol=lf

# systemd unit files must stay LF (CRLF breaks unit parsing on Linux).
*.path text eol=lf
*.service text eol=lf
*.timer text eol=lf
